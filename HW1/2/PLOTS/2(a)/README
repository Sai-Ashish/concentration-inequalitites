Plot 2(a) shows that blue curve is upper bounded by red curve by choosing gaussian with higher variance than the chosen blue curve gaussian

Plot Gaussian2(a) has the reference gaussian with same variance as the chosen gaussian.

Both are valid reference gaussian